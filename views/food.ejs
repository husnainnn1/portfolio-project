<!DOCTYPE html>
<html lang="en">

<head>
  <!--
    Metadata:
    - Specifies the document type and language as English.
    - Meta charset ensures proper text encoding (UTF-8 for universal character support).
    - Meta viewport ensures the webpage is responsive and scales properly on all devices.
    - Title describes the page as "Add Fast Food Item."
    - Tailwind CSS is included for utility-based styling.
  -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Fast Food Item</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-r from-blue-500 to-teal-500 font-sans">
  <!--
    Body:
    - The background is styled with a gradient (blue to teal) for a visually appealing look.
    - Tailwind's `font-sans` ensures a clean sans-serif font.
  -->
  <div class="container mx-auto py-20">
    <!--
      Form Container:
      - Positioned in the center using `container` and `mx-auto`.
      - `py-20` adds vertical spacing for aesthetic spacing on the page.
      - A card-style form is created with a white background, rounded corners, and shadow effects.
    -->
    <div class="max-w-lg mx-auto bg-white rounded-xl shadow-xl p-8">
      <!--
        Form Heading:
        - Highlights the purpose of the page with a bold and centered heading.
        - `text-blue-700` ensures it stands out against the white background.
      -->
      <h1 class="text-4xl font-extrabold text-center text-blue-700 mb-8">Add a New Fast Food Item</h1>

      <!--
        Form:
        - Action sends data to "/fastfood/add" using the POST method.
        - Includes `enctype="multipart/form-data"` for file uploads (image input).
      -->
      <form action="/fastfood/add" method="POST" enctype="multipart/form-data">
        <!-- Title Field -->
        <div class="mb-6">
          <label for="title" class="block text-lg font-semibold text-gray-700 mb-2">Title</label>
          <input type="text" id="title" name="title" required
            class="w-full px-6 py-3 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all">
        </div>

        <!-- Content Field -->
        <div class="mb-6">
          <label for="content" class="block text-lg font-semibold text-gray-700 mb-2">Content</label>
          <textarea id="content" name="content"
            class="w-full px-6 py-3 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all"
            rows="4"></textarea>
        </div>

        <!-- Price Field -->
        <div class="mb-6">
          <label for="price" class="block text-lg font-semibold text-gray-700 mb-2">Price</label>
          <input type="number" id="price" name="price" required
            class="w-full px-6 py-3 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all">
        </div>

        <!-- Stock Field -->
        <div class="mb-6">
          <label for="stock" class="block text-lg font-semibold text-gray-700 mb-2">Stock</label>
          <input type="number" id="stock" name="stock" required
            class="w-full px-6 py-3 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all">
        </div>

        <!-- Image Upload Field -->
        <div class="mb-6">
          <label for="image" class="block text-lg font-semibold text-gray-700 mb-2">Image URL</label>
          <input type="text" id="image" name="image" placeholder="Enter image URL"
            class="w-full px-6 py-3 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all">
        </div>

        <!-- Submit Button -->
        <div class="text-center">
          <button type="submit"
            class="w-full px-6 py-3 bg-blue-600 text-white text-lg font-semibold rounded-md shadow-md hover:bg-blue-700 transform transition duration-300 hover:scale-105">
            Add Fast Food Item
          </button>
        </div>
      </form>
    </div>
  </div>
</body>

</html>