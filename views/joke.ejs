<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Joke</title>
  <link href="https://cdn.tailwindcss.com" rel="stylesheet">
</head>
<body class="bg-blue-50 font-sans">
  <div class="container mx-auto py-10">
    <h1 class="text-4xl font-bold text-center text-blue-700 mb-6">Here's a Joke for You!</h1>
    <div class="bg-white rounded-lg shadow-md p-6">
      <% if (joke.type === 'single') { %>
        <p class="text-lg"><%= joke.joke %></p>
      <% } else { %>
        <p class="text-lg"><strong>Setup:</strong> <%= joke.setup %></p>
        <p class="text-lg"><strong>Punchline:</strong> <%= joke.delivery %></p>
      <% } %>
    </div>
  </div>
</body>
</html>
