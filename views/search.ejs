<!DOCTYPE html>
<html lang="en">

<head>
  <!-- 
    Metadata:
    - Specifies the document type and sets the language to English.
    - Meta charset ensures proper text encoding (UTF-8) for universal character support.
    - Meta viewport ensures the page scales and adapts properly on all devices.
    - Title describes the page as "Search Fast Foods."
    - Tailwind CSS is included for utility-based styling.
  -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Fast Foods</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 font-sans flex flex-col items-center justify-center min-h-screen py-8">
  <!-- 
    Body:
    - Light gray background (`bg-gray-50`) provides a clean and neutral design.
    - Tailwind's `font-sans` ensures the use of a modern sans-serif font.
    - Flexbox layout centers the content vertically and horizontally.
    - `min-h-screen` ensures the body takes up the full height of the viewport.
    - `py-8` adds padding for spacing at the top and bottom.
  -->

  <!-- Main Container -->
  <div class="bg-white w-full max-w-xl p-6 rounded-xl shadow-lg">
    <!-- 
      Container:
      - White background (`bg-white`) provides contrast against the light gray body background.
      - Maximum width (`max-w-xl`) ensures readability on all screen sizes.
      - Padding (`p-6`), rounded corners (`rounded-xl`), and shadow (`shadow-lg`) enhance the design.
    -->

    <!-- Heading -->
    <h1 class="text-4xl font-semibold text-center text-gray-800 mb-6">Search Fast Foods</h1>
    <!-- 
      Heading:
      - Bold, large text (`text-4xl font-semibold`) to draw attention.
      - Centered alignment (`text-center`) ensures prominence.
      - Dark gray color (`text-gray-800`) for contrast and readability.
      - Bottom margin (`mb-6`) separates the heading from the search form.
    -->

    <!-- Search Form -->
    <form action="/search" method="POST" class="flex items-center mb-6">
      <!-- 
        Search Input:
        - Full-width (`w-full`) text input with padding (`py-2 px-4`) for a comfortable user experience.
        - Border styling (`border-gray-300`) ensures subtle visibility.
        - Focus effects (`focus:ring-2 focus:ring-blue-500 focus:border-blue-500`) improve accessibility.
        - Rounded left corners (`rounded-l-md`) provide a cohesive look with the adjacent button.
      -->
      <input type="text" name="query" placeholder="Search for fast food..." required
        class="w-full py-2 px-4 border border-gray-300 rounded-l-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-lg" />
      <!-- 
        Search Button:
        - Red background (`bg-red-600`) matches the theme of fast food branding.
        - Text is white for contrast, with hover and focus effects (`hover:bg-blue-600 focus:ring-blue-500`).
        - Rounded right corners (`rounded-r-md`) complement the input field.
      -->
      <button type="submit"
        class="bg-red-600 text-white shadow-lg py-2 px-4 rounded-r-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 text-lg">
        Search
      </button>
    </form>

    <!-- Search Results Section -->
    <div class="max-h-96 overflow-y-auto">
      <!-- 
        Results Container:
        - Limited height (`max-h-96`) with vertical scrolling (`overflow-y-auto`) for usability.
        - Results are displayed in a list with spacing between items (`space-y-4`).
      -->
      <ul class="space-y-4">
        <% results.forEach(food => { %>
          <!-- Individual Result Item -->
          <li class="p-4 bg-gray-100 rounded-lg shadow-sm hover:bg-blue-50">
            <!-- 
              List Item:
              - Each result is displayed as a card with padding (`p-4`), light gray background (`bg-gray-100`), and rounded corners.
              - Hover effect (`hover:bg-blue-50`) highlights the item when interacted with.
            -->
            <strong class="text-xl font-semibold text-gray-700"><%= food.title %></strong>
            <!-- Title:
              - Bold and larger text (`text-xl font-semibold`) to emphasize the food title.
              - Dark gray color (`text-gray-700`) ensures readability.
            -->
            <p class="mt-2 text-gray-600 text-base"><%= food.content %></p>
            <!-- Description:
              - Slightly smaller text (`text-base`) for the food description.
              - Light gray color (`text-gray-600`) differentiates it from the title.
            -->
            <p class="mt-2 text-gray-700 text-lg">Price: $<%= food.price %></p>
            <!-- Price:
              - Bold text (`text-lg`) with dark gray color to highlight the price.
            -->
            <p class="mt-2 text-gray-600">Stock: <%= food.stock %> available</p>
            <!-- Stock:
              - Additional information about the availability of the item.
              - Styled in light gray to make it less prominent than the title or price.
            -->
            <img src="<%= food.image %>" alt="<%= food.title %>" class="mt-4 w-full h-48 object-cover rounded-md">
            <!-- Image:
              - Displays the food image with a fixed height (`h-48`) and rounded corners.
              - `object-cover` ensures the image maintains its aspect ratio while filling the container.
            -->
          </li>
        <% }) %>
      </ul>
    </div>
  </div>

</body>

</html>

